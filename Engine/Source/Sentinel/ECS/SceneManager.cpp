#include "stpch.h"
#include "Sentinel/ECS/SceneManager.h"

namespace Sentinel {
    void SceneManager::LoadFromFile(const STL::string& filePath) {
    }

    void SceneManager::SaveToFile(const STL::string& filePath, Scene* scene) {
    }
}  // namespace Sentinel